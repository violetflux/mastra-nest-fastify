# AGENTS.md

## 交流

- 回复中文。

## 编码约定

- 源码导入不写 `.ts` 或 `.js` 后缀。
- 对外 API 和不直观的生命周期逻辑补充简洁注释。
- 函数参数、对象解构或泛型声明过长时主动换行，每个字段单独一行。
- 适配器只负责 NestJS 与 Fastify 生命周期，不引入业务鉴权、模型或存储逻辑。
- 写完代码不执行 build，只运行必要的类型检查、测试和 ESLint。
